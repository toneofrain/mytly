spring:
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${LOCAL_DB_URL}
    username: mystly
    password: ${LOCAL_DB_PW}
